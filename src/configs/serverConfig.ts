/**
 * Server configuration variables.
 */
const serverConfig = {
	appName: 'myApp',
	port: 3000,
	host: 'localhost',
	env: 'dev',
	dbUri: 'mongodb://localhost:27017/',
}

export default serverConfig;